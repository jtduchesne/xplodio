import React from "react";
import styled from "styled-components";

const Home = () => {
  return (
    <Wrapper>
      Home
    </Wrapper>
  );
};

const Wrapper = styled.div`
`;

export default Home;
